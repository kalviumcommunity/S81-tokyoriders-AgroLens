# models/

This folder stores persisted ML artifacts (trained models, fitted preprocessors, etc.).

- Do not commit large artifacts unless required by your workflow.
- Artifacts should be created by the training pipeline and loaded by the prediction pipeline.
