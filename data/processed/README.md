# processed/

Store derived datasets created from `raw/`.

Rules:
- Save cleaned, merged, and feature-engineered data here.
- Keep versioned names, for example `processed_weather_v1.csv`.
- Regenerate from scripts; avoid manual spreadsheet edits.
