# outputs/reports/

Legacy folder.

Current pipeline outputs are written to the repository-level `reports/` folder.
If you find older files here, they can usually be regenerated by running:
- `python -m src.main train`
- `python -m src.main predict --data-path data/raw/source_demo_crops_20260321.csv --target-column yield_kg`
